apiVersion: v1
kind: Service
metadata:
  name: task-mcp-service
  namespace: task-manager
  labels:
    app: task-mcp
spec:
  selector:
    app: task-mcp
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
