# =============================================================================
# Build - task-mcp (CI + Registry only)
# =============================================================================
# Purpose:
#   Builds the task-mcp Docker image and pushes it to GitHub Container
#   Registry (ghcr.io/asifalishaikh/task-manager-agent/task-manager-mcp).
#
# This is CI + delivery to registry — NOT full CD (no auto-deploy to a
# live server). Full CD (auto-deploy to K8s) will be added in Part 3.
#
# Triggers:
#   - Push to master that changes services/task-mcp/** or this workflow file
#   - Pull request to master that changes services/task-mcp/**
#
# Tags pushed:
#   - Branch name (e.g., master)
#   - Commit SHA (e.g., sha-a1b2c3d)
#   - Semver tag (e.g., v0.1.0) when a git tag is pushed
#
# Pull requests: build only, no push to registry.
# =============================================================================
name: Build - task-mcp

on:
  push:
    branches: [master]
    paths:
      - 'services/task-mcp/**'
      - '.github/workflows/task-mcp-build.yml'
  pull_request:
    branches: [master]
    paths:
      - 'services/task-mcp/**'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/task-manager-mcp

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=sha,format=short
            type=ref,event=branch

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: services/task-mcp
          file: services/task-mcp/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
